version: 1
frontend:
  phases:
    preBuild:
      commands:
        - node --version
        - yarn --version
        - yarn install --frozen-lockfile
    build:
      commands:
        - yarn build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
